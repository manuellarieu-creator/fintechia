const mysql = require('mysql2/promise'); require('dotenv').config(); async function patch() { const conn = await mysql.createConnection({ host: process.env.DB_HOST || 'localhost', user: process.env.DB_USER || 'root', password: process.env.DB_PASSWORD || '', database: process.env.DB_NAME || 'fintechia' }); console.log('Connected to DB'); try { await conn.query('ALTER TABLE kyc ADD COLUMN document_verso_url VARCHAR(500) AFTER document_url'); console.log('document_verso_url added'); } catch(e) { console.log(e.message); } try { await conn.query('ALTER TABLE kyc ADD COLUMN motif_rejet VARCHAR(500) AFTER statut'); console.log('motif_rejet added'); } catch(e) { console.log(e.message); } await conn.end(); } patch().catch(console.error);
