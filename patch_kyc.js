const db = require('./backend/config/db'); async function patch() { console.log('Connected to DB'); try { await db.query('ALTER TABLE kyc ADD COLUMN document_verso_url VARCHAR(500) AFTER document_url'); console.log('document_verso_url added'); } catch(e) { console.log(e.message); } try { await db.query('ALTER TABLE kyc ADD COLUMN motif_rejet VARCHAR(500) AFTER statut'); console.log('motif_rejet added'); } catch(e) { console.log(e.message); } process.exit(0); } patch().catch(console.error);
